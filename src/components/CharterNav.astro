---
import type { CharterBlock } from '../lib/charter'

interface Props {
  blocks: CharterBlock[]
}

const { blocks } = Astro.props
---
<nav class="charter-nav">
  {
    blocks.map((b) => (
      <a href={`#article-${b.id}`}>{b.heading}</a>
    ))
  }
</nav>
