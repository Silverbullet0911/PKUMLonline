---
interface Props {
  personalHonors?: string[]
  teamHonors?: string[]
}

const { personalHonors = [], teamHonors = [] } = Astro.props
const all = [...teamHonors, ...personalHonors]
---
{all.length > 0 && <span class="honors">{all.join('、')}</span>}
